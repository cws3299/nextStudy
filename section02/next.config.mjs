/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
};

export default nextConfig;

// reactStrictMode => react의 strict모드처럼 에러 확인을 위해 두번씩 실행하는 작업, 개발할때는 일단 false로 설정
